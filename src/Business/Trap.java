package Business;

public class Trap extends Enemy{
    protected Trap(char character, String name, int healthCapacity, int attackPoints, int defensePoints) {
        super(character, name, healthCapacity, attackPoints, defensePoints);
    }
}
